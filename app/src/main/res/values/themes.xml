<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Определяем основную тему для приложения -->
    <style name="Theme.SimpleApp" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primaryDark</item> <!-- Темный основной цвет -->
        <item name="colorAccent">@color/accentColor</item> <!-- Цвет акцента -->
        <item name="android:colorBackground">@color/background</item>
        <item name="android:textColor">@color/textColor</item>
    </style>

</resources>
